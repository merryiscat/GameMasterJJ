"""
파이프라인 설정값

모델명, 경로, 배치 크기 등 설정을 한곳에서 관리한다.
"""

from pathlib import Path

# ============================================================
# 경로 설정
# ============================================================
# 프로젝트 루트 (scripts/rules/config.py → 두 단계 위)
PROJECT_ROOT = Path(__file__).parent.parent.parent

# 룰북 파일이 저장된 디렉토리
RULES_DIR = PROJECT_ROOT / "rules"

# ChromaDB 저장 경로
CHROMA_DIR = str(PROJECT_ROOT / "chroma_db")

# LLM 프롬프트 템플릿 디렉토리
PROMPTS_DIR = Path(__file__).parent / "prompts"

# ============================================================
# OCR 설정 (Upstage Document Parse)
# ============================================================
UPSTAGE_API_URL = "https://api.upstage.ai/v1/document-digitization"
UPSTAGE_OCR_MODE = "force"  # 모든 파일에 OCR 강제 적용
UPSTAGE_MODEL = "document-parse"

# ============================================================
# LLM 모델 설정 (OpenAI, 2026.04 기준)
# gpt-4o는 2026.2.13 퇴역됨. 사용 금지.
# ============================================================
TRANSLATE_MODEL = "gpt-4.1"           # 번역 (비용 효율 + 높은 품질)
PARSE_MODEL = "gpt-5.4-mini"         # 소스별 개별 파싱 + 섹션별 취합
PARSE_VLM_MODEL = "gpt-5.4-mini"     # VLM 파싱 (이미지+텍스트)
PREPROCESS_MODEL = "gpt-5.4-mini"    # 섹션 정리 + 플레이북 생성
QA_MODEL = "gpt-5.4-mini"            # QA 쌍 생성

# ============================================================
# 임베딩 설정 (ChromaDB)
# ============================================================
EMBEDDING_MODEL = "text-embedding-3-small"
BATCH_SIZE = 100  # ChromaDB 배치 추가 단위

# ============================================================
# 번역 설정
# ============================================================
TRANSLATE_CHUNK_SIZE = 4000  # 번역 시 텍스트 분할 크기 (글자 수)

# ============================================================
# 유튜브 설정
# ============================================================
# 룰 설명 품질 높은 채널 화이트리스트
YOUTUBE_WHITELIST_CHANNELS = [
    "보드엠",
    "다이게이머",
]

# ============================================================
# 나무위키 설정
# ============================================================
NAMUWIKI_BASE_URL = "https://namu.wiki"

# ============================================================
# 블로그 설정
# ============================================================
BLOG_SEARCH_KEYWORDS = ["규칙", "룰", "룰북", "하는법"]
